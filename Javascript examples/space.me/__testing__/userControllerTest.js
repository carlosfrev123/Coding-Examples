const server = "http://localhost:3000";
const request = require("supertest");
const fs = require("fs");

describe("User Creation", () => {
  it("responds with 200 status code and an object.", () => {
    //Dummy data
    const username = `Diane${Date.now()}`;
    const password = "Diane";

    
  });
});
